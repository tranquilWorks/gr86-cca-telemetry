# RVB22 reconstructed firmware: programming and calibration

Controlling source identity: **2.1.4-revb-recovered-20260909**. These files reconstruct the intended pre-loss RVB22 corrections from the restored 2.1.3 baseline. `RECOVERY_NOTES.md` distinguishes historical evidence. This source is not a flashed unit, compiled target image, or native driver timing pass.

Build with the included `build.sh`, Arduino-ESP32 3.3.6 and NimBLE-Arduino 2.3.6. The configured ESP32-S3 target uses 160 MHz via the pinned FQBN, 8 MB QIO flash, enabled PSRAM, UART0 debug and USB CDC disabled. The source initializes NimBLE at +3 dBm and has no Wi-Fi initialization. Raising CPU frequency, BLE power, or enabling Wi-Fi invalidates the C04 release thermal allocation. The profile constrains the shipped build; it is not a hardware current limiter. CAN remains listen-only at 500 kbit/s with no TX API or TX queue; hardware R301 must remain DNP/open. Run `CCA_BASELINE_ROOT=<restored GR86_CCA_RevB> python tests/check_release.py --source-only` for source checks. Run the check without that option only after producing the real split/merged images and ELF. Do not create placeholder binary files or reuse the 2.1.2 image with this identity.

Use the controlled target-powered Tag-Connect adapter and normal power protection. VTREF is sense-only; adapter outputs must be gated off while target power is absent. Hold GPIO0 low through the supervisor recovery and at least 3 ms after actual EN rises until ROM download starts. A manual EN sink must establish at least 50 microseconds low. Actual adapter timing and interrupted-flash recovery remain unit tests. Exact image offsets remain bootloader 0, partitions 0x8000, boot_app0 0xe000, application 0x10000. Record unit identity and actual flashed-image readback; source hashes do not establish flash contents.

Oil acquisition runs every 10 ms with alpha 0.5. Fresh default and RESETCFG publication are 20 ms. Saved runtime schema1 RATE values are intentionally retained, including slower values; RATE supports 10–2000 ms. The 50 ms oil-event estimate requires RATE <=20 ms, phone request interval <=20 ms, the full service/acquisition contract <=5 ms and accepting transport. A 20 ms pressure event has no guaranteed visibility. SHOW STATS reports measured service/acquisition gaps and violations. A violation above 5000 microseconds sets oil fault bit7, clears valid pressure and publishes 0xFFFF before further CAN forwarding. Timing/electrical fault recovery requires three consecutive valid acquisitions. Startup has no synthetic pre-service gap. These checks expose overruns; they do not establish opaque Arduino, NVS, ADC or NimBLE worst-case execution time.

The unchanged 120 notifications/s budget includes oil50/s, GPS10/s, time1/s and diagnostics0.5/s, leaving 58.5/s average for physical CAN. Six physical IDs requested at10 Hz require60/s, so at least1.5/s coalescing is expected in steady state. Oil and diagnostic cache entries are protected from physical-ID eviction. Synchronous notification rejection retains the latest state and invokes100 ms backoff; a short pulse lost during that outage cannot be reconstructed. Actual phone/radio delivery remains a separate condition.

Sensor: Honeywell MIPAN2XX150PSAAX, DigiKey480-MIPAN2XX150PSAAX-ND. Its150 psi sealed-gauge reference is14.7 psia, transfer is10–90% of excitation and excitation range is4.75–5.25 V. Reported pressure is sealed gauge: P=150*(Vsig/Vexc−0.1)/0.8. Ambient gauge would require Pambient: Pgage=Psealed+14.7−Pambient. Never infer zero at key-on; engine oil pressure may be nonzero, and a sealed sensor at altitude is not a zero-ambient-gauge reference. Underpressure diagnostic transition is unspecified. The1 ms response is typical10–90%, not a maximum; the separately listed7 ms startup is not a hydraulic response bound.

Verify both ADC channels independently with measured connector voltage, then use CAL VERIFY with a traceable0.001–0.025 V worst-case connector-domain residual. The independent propagated ratio error must still be <=0.006; for complete calibrated spans below0.8, its ceiling scales downward as0.006*span/0.8 to preserve the1.125psi electronic-error allocation. CAL 0 and CAL 1 require known0/150 psi sealed-gauge references (14.7/164.7 psia), not vented ambient or an assumed stopped-engine state. Endpoint acceptance is r0=0.08–0.12 and r150=0.88–0.92; minimum permitted calibrated span0.76. Nominal pressure mode requires the exact0.1/0.9 pair. Widening the diagnostic window to0.08–0.92 accommodates the sensor and admitted measurement uncertainty; hard thresholds0.06/0.94 and the separate ratio-accuracy gate are unchanged. A valid zero is numeric0; a fault is0xFFFF, never zero pressure. Plausible in-range stuck input is not identifiable without an independent reference.

Remote excitation checking subtracts6 mV in addition to local ADC uncertainty: two wires at <=0.2 ohm each and a15 mA engineering current allocation. The manufacturer6.5±1 mA is specified at25°C unless noted;15 mA is an assumed engineering operating ceiling, not a documented hot/startup maximum. Actual wiring/contact current and reference uncertainty remain conditions.

GPS requests GPS-only, SBAS off, DGPS none, RMC/GGA only,115200 baud and10 Hz for the selected GPS L1 external accessory. PA1616D SBAS mode is limited to5 Hz. `configured=yes` requires positive acknowledgments for mode353/313/301 and rate220, SBAS513 readback0, and ten distinct dated100 ms RMC advances at115200 baud. Unseen ACK255 is not success. Duplicate/backward/stale solutions cannot refresh old position age. Whole framed commands are deferred until UART capacity can accept all bytes; command index/state does not advance on deferral or short write. Exact module firmware behavior, PPS-to-UTC association and actual accessory bias/RF performance remain unobserved.

The Timurr ASC tap does not supply0x13A or0x6FC. Their whitelist entries remain available for other taps and must not be treated as evidence those frames exist at ASC. The bridge forwards raw bytes; a whitelist comment is not a verified signal decoder or model-year qualification.

After any source edit, rerun the reconstructed host checks and regenerate the source manifest. After any final hardware change, rebind affected analog/return/power models. No original physical acceptance criterion is silently replaced with a source PASS.
